<form method="post" action="{{ path('app_devis_produit_delete', {'id': devis_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devis_produit.id) }}">
    <button class="btn">Delete</button>
</form>
