<form method="post" action="{{ path('app_facture_produit_delete', {'id': facture_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture_produit.id) }}">
    <button class="btn">Delete</button>
</form>
