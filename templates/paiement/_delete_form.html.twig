<form method="post" action="{{ path('app_paiement_delete', {'id': paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement.id) }}">
    <button class="px-4 py-2 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-md shadow-md ml-100">Delete</button>
</form>